{
  "name": "sitemap-content-inventory",
  "source": "https://www.sitemaps.org/protocol.html",
  "approach": "public-sitemap-xml",
  "runtime_tier": 1,
  "description": "Read public XML sitemaps and normalize URL inventory rows for SEO, content, and competitor monitoring.",
  "business_value": "No-key competitor content inventory and landing-page monitoring.",
  "inputs": [
    {
      "name": "sitemap_url",
      "required": true,
      "type": "string"
    },
    {
      "name": "limit",
      "required": false,
      "type": "int",
      "default": 100
    }
  ],
  "outputs": [
    "sitemap_url",
    "url",
    "last_modified",
    "change_frequency",
    "priority",
    "path",
    "section",
    "inferred_page_type",
    "monitor_reason"
  ],
  "auth": {
    "type": "none",
    "note": "Uses public sitemap XML."
  },
  "endpoints": [
    "GET {sitemap_url}"
  ],
  "pagination": {
    "type": "sitemap-index",
    "fields": ["sitemapindex", "urlset", "limit"]
  },
  "verification_command": "python3 apiified/sitemap-content-inventory/script.py --sitemap-url https://www.shopify.com/sitemap.xml --limit 100 --output sitemap-pages.json",
  "verification_result": "Live smoke checked Shopify sitemap.xml and received URL rows without an API key.",
  "known_risks": [
    "Sitemaps can omit pages.",
    "Large sitemap indexes need recursion caps.",
    "Public sitemap availability varies by site."
  ]
}
